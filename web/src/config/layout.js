// ============================================================
// 布局配置（下游可覆盖）
// ============================================================

// 侧边栏（MainLayout 的 n-layout-sider）默认折叠状态。
//
// 仅在用户本地没有偏好记录时生效；用户手动收/展后，偏好会写入
// localStorage（key: `app.sidebar-collapsed`），此后以用户偏好为准，
// 本默认值不再影响。想恢复本默认值，清除该 key 即可。
//
// 上游默认折叠；下游若希望首屏展开，改为 false 即可。
export const SIDEBAR_DEFAULT_COLLAPSED = true
