# Design: internship-work-review-docs

## Context

- 目标读者：**自己**。用途：① 工作汇报底稿；② 为后续继续推进数字人方向的工作提供素材；③ 循实习经验提炼可发表文章的选题依据。写作取向：事实与数字优先，每个结论给证据锚（代码路径/commit/指标表），区分"已验证/待验证/已被否证"。
- 素材四源：CyberVerse（`~/code/CyberVerse` @ `4968280`）、InternWiki（已复制至 `management/docs/knowledge/`）、博客精选（已复制）、InternWiki 项目树（`projects/digital-human/tasks.json`）。
- 放置规则（用户明确要求）：子文件夹夹内平铺、禁止再嵌套。两个子文件夹：`实习复盘/`（7 篇）、`论文笔记/`（统一笔记库）。
- 内容文档（非索引/汇总类）统一写作骨架，**不是单纯写"我们改了什么"**：
  1. `为什么重要` — 这部分在数字人系统里为什么关键、不做会怎样
  2. `现行做法` — 该方向的信息总结：主流算法一般怎么做、有哪些流派与代表工作
  3. `我们的工作` — 我们做了哪些实验/实现/结论（含否证）
  4. `可能的改进方向` — 基于以上，可尝试的改进方向（开放问题）

## Goals / Non-Goals

**Goals:**
- 8 篇复盘 + 论文笔记，每篇内容文档按统一骨架成文（重要→现行→我们的→改进方向）
- 支撑工作汇报：量化成果总表可直取直用
- 支撑后续推进：遗留问题、未采纳方案、下一步补实验清单清晰可执行
- 产出《发文方向》：每条候选选题一张"证据资产负债表"

**Non-Goals:**
- 不做团队交接文档（读者是自己）
- 不复制/搬运 knowledge/ 已有内容，只做第一方复盘并引用
- 不虚构数字，查不到的写"待补"

## 文档登记表（唯一权威，均在 `management/docs/实习复盘/` 下平铺）

> 双层流程：每篇由独立单篇 change（`docs-intern-<slug>`）实施——先完成「资料整理」任务组（产物先过目），再启动「动笔写作」任务组。本总 change 只管体系与验收，不写正文。

| # | 文件 | 职责边界（只写什么/不写什么） | 依赖 | 单篇 change |
|---|------|------------------------------|------|-------------|
| 1 | `数字人介绍与技术路线.md` | 数字人概览 + 主要技术路线（合并原"要点"与"技术路线"）：数字人是什么、为什么重要、**技术路线版图**（**「动作 × 身份」两因素框架** + 动作生成方式 + 身份呈现后端 + 两者的绑定关系 + 完整路线清单）、**评价体系**（三维度 + 五指标族 + 对应关系）；不写我们的具体工程/实验（链各专题） | — | `docs-intern-intro` |
| 2 | `CyberVerse框架.md` | 只写框架介绍：为什么需要统一框架、现有开源框架版图、CyberVerse 架构（三服务/插件体系/模型接入/部署形态）、我们的改造与扩展、改进方向；不写单个模型的算法细节（链论文笔记） | 1 | `docs-intern-cyberverse` |
| 3 | `数字人身份.md` | 只写身份主题：为什么重要、现行身份表示与注入做法、我们的漂移实验（模长/夹角）与身份资产研究、改进方向 | — | `docs-intern-identity` |
| 4 | `数字人动作.md` | 只写动作主题：为什么重要、分层（①音唇同步 ②表情×语言交互 ③手部/全身）现行做法、我们的实验、改进方向；不写渲染与实时性 | — | `docs-intern-motion` |
| 5 | `工程改进.md` | 工程改进全集：系统级基建（瓶颈定位、音频缺口、僵尸会话、Ditto TRT/RTF、编码链路、silent-avatar）+ PasteBack 贴回技术 + **实时性改进**（首帧/开口、打断收声、待机静默、画质粒度）+ 未采纳方案 | — | `docs-intern-engineering` |
| 6 | `数字人行业全景.md` | 只写行业与产品视角：模型横评（15+ 第一手测试数据）、竞品与产品调研、选型结论、趋势判断；技术原理与路线放《数字人介绍与技术路线》，此处只引用 | 1、2 + 论文笔记五篇深读 | `docs-intern-industry` |
| 7 | `总结.md` | 总览与发文方向合一：核心工作导览与量化成果总表（数字从 1–6 汇总，不新造证据）+ 时间轴+方法沉淀 + 选题漏斗、候选方向证据资产负债表、related-work 撞车扫描、决策建议 | 1–6 | `docs-intern-summary` |

### 1. `数字人介绍与技术路线.md`（结构契约）

- `## 数字人是什么`：定义、系统组成（动作 / 身份 / 渲染合成 / 实时性四要素）、与相关领域边界
- `## 为什么重要`：应用场景与产业价值、面试官数字人等落地形态
- `## 主要技术路线`：三条路线各自的驱动信号→表示→生成→渲染做法 + 代表模型清单（papers 库互链）+ 优缺点对比表
  - **视频基座模型路线**（wan-streamer、hallo3 等）
  - **动作空间扩散路线**（Ditto、AvatarForcing 系）
  - **3D GS 路线**（UIKA、GAGAvatar、LHM 等）
- `## 评价体系`：先定义**评价维度**（从哪个层面评）与**评价指标**（用哪个数字量评）两个术语，再分三节——三个评价维度、五个评价指标族（sync_c/sync_d、csim/psnr/ssim/lpips/topiq_fr、RTF/延迟 等各回答什么问题）、两者的对应关系表（并指出「表现范围」缺通用量化指标）；**动作与身份两个因素分别对应到指标族**（动作→唇同步/动作自然度；身份→CSIM/画质）

### 2. `CyberVerse框架.md`（结构契约）

- `## 为什么需要框架`：模型碎片化（每个 avatar 模型接口/依赖不同）、实时 Agent 需要 ASR/LLM/TTS/Avatar 编排、会话与媒体链路
- `## 现行做法`：开源框架版图与取舍（OpenAvatarChat / LiteAvatar 等，引 knowledge/open-avatar-chat-liteavatar、cyberverse-realtime-digital-human-agent）
- `## CyberVerse 架构`：三服务（Python inference gRPC :50051 / Go orchestrator :8080 + TURN :8443 / Vue 前端 :5173）、插件体系（`inference/plugins/{asr,llm,tts,voice_llm,avatar}` + `proto/*.proto`）、AvatarPlugin / BidirectionalAvatarPlugin 抽象、RAG 与 PersonaAgent+SubAgent
- `## 模型接入`：models/ 下 AvatarForcing、Ditto、FlashHead、MuseTalk、SoulX-LiveAct 的接入形态（链论文笔记五篇）
- `## 部署形态`：远程 GPU 服务器 + SSH 隧道（5173/8080/8443）、本地只编辑/远端只部署的协作纪律（引其 AGENTS.md）
- `## 我们的改造与扩展`：工程改进与实时性改进的落点（链《工程改进》）、管理模块迁移、[shared] 脚手架协作
- `## 可能的改进方向`

### 3. `数字人身份.md`（结构契约）

- `## 为什么重要`：身份漂移对可用性的影响（长视频、对话场景）
- 以下均为二级标题（无桶标题）：
  - `## 身份表示与渲染后端`（**合并节，按主要路线叙述**）：开头一句"表示决定渲染器能吃什么、渲染器决定身份怎么落地"；结尾一张跨路线对照表（路线 / 身份表示 / 渲染后端 / 身份怎么被保持 / 换身份代价）；正文按路线分小节点名代表模型——2D 换嘴（MuseTalk、SadTalker）、**动作空间 + 快速渲染器（我们的主线，写最细：隐式关键点 + warping = LivePortrait / Ditto / MegaPortraits 血统；外观特征 + flow-warp = LIA-X；appearance code + 加法解耦 = FLOAT / Avatar Forcing）**、视频基座模型（VASA-1、HunyuanVideo-Avatar、OmniAvatar）、3D 资产学习式（GaussianTalker / GAGAvatar / UIKA / FlexAvatar，非重点）、3D 资产参数化装配式（FLAME / ARKit / Audio2Face）、掩码局部多人控制
  - `## 身份注入与保持`：跨路线清单（加法解耦 → 推理期锚点引导 → 训练期条件化 → 区域抑制）；不重复逐路线描述
  - `## 一致性度量`：主用 CSIM / id-sim 与派生漂移指标；写清尺子出身（从人脸识别借来、无标准化协议）、五种失效场景一张表（照片域依赖 / 时序漂移 / 环境不鲁棒 / 长时演变 / 风格化不可度量）、一把尺子身兼裁判-教练-奖励-球员四职；三条用法结论（只作必要条件、必须配人工检查、跨域场景声明边界）；完整论证归论文笔记
  - `## 漂移诊断：Avatar Forcing 上的两个假说先测后治`：**先交代实验载体**（Avatar Forcing 流式路径、被测量的 motion latent、身份侧固定为 appearance code、c1 单身份 300 秒，接入细节链 `docs-note-avatar-forcing`）→ 模长实验被否证 → 夹角实验仅在单身份上得到支持
  - `## 治理尝试与裁决`：**同一载体（Avatar Forcing）上的三种手段**——A1 锚点引导 GO / 参考条件化 v1 失败与 v2 归档失败 / Learned Anchor Force 终止
  - `## 身份资产与参考提供`：单图/多图/视频参考、anchor banks、注入时机与成本
  - `## 微调类身份提升尝试`：ArcFace 系身份特征/loss 的实验（证据状态：知识库/博客/代码三处均无记录 → 口述待补）
- `## 可能的改进方向`：多身份夹角验证、治理消融、身份资产协议标准化
- `## 汇报要点`：一页可直取的结论

### 4. `数字人动作.md`（结构契约）

- `## 为什么重要`：动作自然度决定可看性；动作是分层递进的能力
- `## 现行做法（分层）`：①音唇同步（wav2lip→musetalk→vasa1 系）②表情与语言内容交互（语义一致性、听态双向生成）③手部与全身动作（co-speech gesture）；每层主流方法与代表工作（papers 库互链）
- `## 动作空间谱系`（**Blendshape 的深度内容在这里**）：表示谱系按「人能读懂多少」排列——3DMM 系数（BFM/FLAME，语义高）/ **Blendshape 系数（ARKit 52 维等，工业标准接口）** / 隐式关键点（LivePortrait，隐式 blendshape）/ latent（Ditto 265 维、AF 20 维）；讲语义可读性、训练来源、可控制性、跨软件通用性（引《动作空间专题》）
- `## 我们的工作`：
  - `### 中文音频适配（桥系列）`：蒸馏桥 → geom（单边 hinge，反塌缩）→ georkd（geom+RKD 关系蒸馏），附 LSE-C/LSE-D 与 sync_c/sync_d 指标表
  - `### 微调方法（Loss 与注入点）`：微调形态谱系（全冻结+桥 / LoRA / 单层解冻）与注入点审计方法；loss 设计如何影响音唇同步（引《微调策略专题》）
  - `### 表情与听态`：表情"太用力"问题、AvatarForcing 双向听说、Ditto 眼嘴四层拆分
- `## 可能的改进方向`：我们做到哪层、市面到哪层、下一步（听态质量、手部接入成本）

### 5. `工程改进.md`（结构契约）

- `## 为什么重要`：实时数字人系统的性能/稳定性天花板
- `## 现行做法`：流式管线与编码链路、GPU 预算分配、会话生命周期管理的通用方案
- `## 我们的工作`：
  - `### 瓶颈定位`：隔离基准 38.5ms/帧 vs 40ms 预算（引《CyberVerse 工程专题》§一）
  - `### 音频缺口修复`：H.264 段尾等待 → shortfall 修复 → 绝对播放锚点（§二）
  - `### 僵尸会话治理`：三层兜底 + 1.4GB 插件泄漏发现（§三）
  - `### Ditto 实时化`：TRT 七步（t4-1~t4-7）、RTF 3→<1→0.75 冲刺（t7）
  - `### 编码与传输链路`：NVENC 硬编、679ms→<100ms（t11/t17）、降分辨率对照（t5）
  - `### PasteBack 贴回技术`：两条路径（AF 裁剪框+alpha vs Ditto M_c2o 仿射）、接缝/波动修复（t11-3、d32fb74）
  - `### 实时性改进`（素材：cyberverse 项目 t10/t13–t25）：为什么重要与延迟预算分解；现行实时化手段（TTFF/FFTT/cadence 口径、预缓冲、常驻编码器、打断处理）；我们的优化——首帧与开口（t15/t16/t20/t25）、打断与收声（t18/t24 四次否证）、待机与静默（t23 A/B、t19、t14）、画质与流畅权衡（t21/t10）
- `## 可能的改进方向`：未采纳方案清单与原因（工程专题 §五）+ 遗留 badcase 与优先级

### 6. `数字人行业全景.md`（结构契约）

- `## 为什么重要`：选型与产品对标决定方向
- `## 我们测过的模型`：15+ 模型 SpeedRun / Formal Eval 横评（RTF、画质、sync_c/sync_d 指标表）——第一手数据
- `## 竞品与产品调研`：阿里云数字人等主流产品（原理/效果/实时通话/延迟），自研 vs 竞品对标
- `## 选型结论`：GAGAvatar/UIKA → 最终体系（CyberVerse + AvatarForcing/Ditto）
- `## 趋势判断`：papers 库 117 篇 + arxiv-digest 时间线（标注推断性质）

### 7. `总结.md`（结构契约，依赖 1–6）

**第一部分：总览**
- `## 实习背景与主线`：方向、目标、时间轴（引 project-tasks.json 里程碑）
- `## 核心工作导览`：身份 / 动作 / 工程改进（含 PasteBack 与实时性改进）/ 行业全景各一段（问题→方法→结果一句话），互链四篇 + 论文笔记
- `## 工作方法沉淀`：任务树管理、实验记录规范（t24 四次否证）、[shared] 脚手架协作
- `## 量化成果总表`：关键数字汇总（逐项标注来源，不新造数字）

**第二部分：发文方向**
- `## 选题漏斗`：从全部工作（身份/动作/工程/评测各线）→ 有增量证据的候选 → 推荐主攻（附淘汰理由）
- `## 候选 A：身份漂移的诊断与治理`
- `## 候选 B：流式 Talking-Head 系统论文`（含 PasteBack 作为质量章节）
- `## 候选 C：数字人评测基准论文`（15+ 模型 + 指标体系，对标 opens2v-nexus）
- `## 候选 D：动作分层与听态生成`
- `## 决策建议`：优先级 + 下一步 30 天补实验清单

## 论文笔记登记表（`management/docs/论文笔记/`，夹内平铺，双层流程同 D0）

> **技术路线不在此夹**：三路线总览只存在于《数字人介绍与技术路线》（复盘 #1），本夹不再设路线篇，避免重复。

统一模板（一篇一个论文/模型）：论文层 `## 是什么` → `## 架构核心`（驱动信号→表示→生成→渲染 + 与同类差异一表）→ **可选工程层** `## 在我们体系中的角色`（插件路径、接入状态、我们的改动/否证；仅深度接入过的模型填写）→ `## 与复盘各线的关联` → `## 参考与延伸`。

| 文件 | 依赖素材 | 单篇 change |
|------|----------|-------------|
| `avatar-forcing.md`（含工程层） | knowledge/《Avatar Forcing 模型精读》《Motion Latent AutoEncoder》《微调实践》+ models/avatarforcing/ @4968280 | `docs-note-avatar-forcing` |
| `ditto.md`（含工程层） | knowledge/《Ditto 模型精读》《Ditto 实时化与 TensorRT 加速复盘》+ models/ditto/ | `docs-note-ditto` |
| `liveact.md`（含工程层） | papers 库 paper-liveact + models/SoulX-LiveAct/ | `docs-note-liveact` |
| `omnimate.md` | papers 库 omnimate-2026 | `docs-note-omnimate` |
| `talker-t2av.md`（含工程层） | knowledge/《Talker-T2AV 模型精读》《Talker-T2AV 接入与验证》 | `docs-note-talker-t2av` |
| `liveportrait.md`（含工程层） | papers 库 `arxiv-2407.03168`（LivePortrait）+ 博客 `paper-liveportrait` + models/ 相关 Stitching/Retargeting 控制 | `docs-note-liveportrait` |
| `lia-x.md`（含工程层） | papers 库 `arxiv-2508.09959`（LIA-X: Interpretable Latent Portrait Animator）+ 博客 `lia-x-2025` | `docs-note-lia-x` |
| 后续新读论文 | 按统一模板新增，命名挂 papers 库条目 | `docs-note-<slug>` |

## Decisions

- **D0 顺序与审核闸门**（依据 doc-writing skill）：
  0. **先 explore**：读该主题相关素材（knowledge/、CyberVerse @4968280、papers 库、任务树），搞清"这篇要写哪些章节、每章有什么内容可用"——**探索先于建 change**
  1. 建单篇 change：proposal / **design（内嵌完整大纲：文章有哪些章节 + 每章写什么内容 + 结论 + 引用关系）** / tasks
  2. **用户审大纲** ← 未过不放行
  3. 「资料整理」：抽取证据与数据表，**在对话中提交审核，不写入仓库**
  4. **用户审素材** ← 未过不放行
  5. 「动笔写作」：正文写进 `management/docs/实习复盘/<标题>.md`
  任一闸门未获明确确认，不得推进到下一步。
- **D1b 禁桶标题**：`## 现行做法` / `## 我们的工作` / `## 概述` 这类**分类名不许当标题**，其下主题直接写成二级标题（骨架只约束写作顺序，不约束标题结构）；主题自身横向展开时才用子节，且子节父标题必须是该主题的名字
- **D1 统一写作骨架**：内容文档按"为什么重要 → 现行做法 → 我们的工作 → 可能的改进方向"组织，避免写成单纯的改进流水账
- **D2 目录与命名**：子文件夹夹内平铺；单篇 change：复盘系 `docs-intern-<slug>`、笔记系 `docs-note-<slug>`
- **D2b 交付物位置**：**仓库里只保留文章正文**（`management/docs/...`）；计划（proposal/design/tasks）留在 change 目录；**素材/整理产物不进仓库**（按 D0 在对话中提交审核）
- **D3 正文不写出处**：文章正文**不写任何出处与素材指引**——不出现 `knowledge/xxx.md`、不写"《X》原文："、不标来源与日期、不标 commit。正文是给读者读的文章，不是带脚注的台账。
  - 出处、素材来源、代码 commit（`@4968280`）只保留在 **change 的整理环节与登记表**里，作为可追溯性；写完不进正文
  - 外部二手信息如果会影响可信度，用**自然语言**说明其性质（如"这是二手整理、未见官方数据"），而不是标出处
  - **例外**：本仓库文档之间的职责边界链接（"详见《数字人身份》"）保留，因为它承担结构分工，不是出处
- **D4 微调内容归属**：微调工作不单独成篇，按主题拆分——身份相关（ArcFace/ID loss 等）入《数字人身份》，音唇同步相关（geom/RKD loss、注入点消融、微调形态谱系）入《数字人动作》；无记录的口述实验标注证据状态，不编造数据
- **D6 图表规范（mermaid）**：架构 / 链路 / 流程 / 谱系类内容**必须配 mermaid 图**（表给细节、图给结构，不重复）；代码块标 `mermaid`，节点文本用双引号包住；单篇 1–3 张，放在对应小节开头
- **D5 数据诚实**：不编造数字；无把握处用自然语言写明（如「推测」「尚无实测」），不使用标注符号体系

## Risks / Trade-offs

- [记忆缺失导致数字空缺] → 标"待补"而非编造；优先引用已归档文档中的数字
- [CyberVerse 仍在演进（t7/t11 active）] → 锚定 commit，注明"截至 2026-08-04 快照"
- [《数字人介绍与技术路线》与《数字人行业全景》路线内容重叠] → 前者写原理与流派（教科书视角），后者写产业/产品/我们实测（调研视角），相互引用不重复展开
- [发文选题误判] → 用 papers 库做 related-work 扫描后再下结论

## Open Questions

（无）
