## MODIFIED Requirements

### Requirement: 左侧文档列表支持子目录折叠

左侧文档列表 SHALL 对带子目录前缀的文档分组显示，并支持分组折叠/展开；折叠状态 SHALL 持久化。

#### Scenario: 折叠子目录
- **WHEN** 文档列表含 `数字人概述/`、`论文笔记/` 等子目录分组，用户点击某分组标题
- **THEN** 该分组下的文档项折叠隐藏，再次点击展开；刷新后保持折叠状态
