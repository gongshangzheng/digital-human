## 1. 前置与素材

- [ ] 1.1 确认 `rename-internship-review-folder` 已落地（`management/docs/数字人概述/` 存在）；未落地则暂停并回报
- [ ] 1.2 复核调研缓存 `.cache/doc-research/{identity,motion,realtime,route}.md` 覆盖 design 第 4 节引用的全部工作（缺哪篇补哪篇）
- [ ] 1.3 抽查 design 里的关键数字（量级、倍数）没有编造；不要求逐条对齐出处

## 2. 写作（按 design 第 4 节逐节产出；**全文无「为什么重要」节**）

- [ ] 2.1 标题下 **3 句无标题导语**（范围声明 / 归类原则 / 口径，约 250 字符）
- [ ] 2.2 `## 一、身份` 六节：一致性 / 多参考注入 / **切分镜与多视角** / 说话风格 / **风格化** / 未解问题（约 11,800 字符，含 1 张 Mermaid + 4 张证据表）
- [ ] 2.3 `## 二、动作`：先写**归类原则**与**难度梯度表**（七级，**不写子问题数目**），再按梯度逐节写——音唇同步 / **跨语言适配**（同级紧随） / 整体面部动力学 / 情绪匹配 / 情绪控制与可编辑性 / 语义 grounding / 双向交流 / 反应性与听态 / 打断与回合边界 / 多人场景 / 手部与全身 / 未解问题（约 14,000 字符，含 5 张证据表）
- [ ] 2.4 `## 三、实时性` 五节：延迟链 / 加速手段 / 长时生成 / **分段边界** / 未解问题（约 7,200 字符，含 1 张 Mermaid + 1 张证据表）
- [ ] 2.5 `## 四、路线` 七节：路线一 2D 换嘴 / 路线二 结构化动作策略 / 路线三 动作扩散 + 快速渲染器 / 路线四 视频基座与整帧 / 路线五 3D 资产（学习式 + 参数化装配式）/ 路线六 掩码多人控制（每节四问：解什么问题 / 怎么做 / 代价与未解 / 独占了什么；**不写“为何不能互相替代”**）（约 8,200 字符，含离线 vs 流式 TTFF 对照）
- [ ] 2.6 `## 五、条件与任务定义` 三节：条件的角色混淆（参考音频 ≠ 驱动音频）/ 任务定义的分裂（T2AV）/ 输入条件不统一（冷启动档位与不可比）（约 3,700 字符，含 1 张证据表）
- [ ] 2.7 `## 六、跨域问题` 三节：数据分布 / 合规与安全 / 复现与口径（约 3,900 字符）
- [ ] 2.8 frontmatter：`title/author/date/tags/order: 20/summary`，与同目录 8 篇格式一致
- [ ] 2.9 **不写汇总性内容**：不写各块的「未解问题」节、不写「问题总账」；正文只到问题节为止

## 3. 口径自查（写完正文后执行一次）

- [ ] 3.1 不搬《介绍与技术路线》的表示谱系表与指标族表（**但路线列表要完整覆盖，不能只取三条**）；不搬《身份》/《动作》/《加速》的实验矩阵与完整数字
- [ ] 3.2 **不引述**：正文无「见《…》」「某篇笔记」「我们的实测表明」这类句式；**正文无站内内链**
- [ ] 3.3 不标来源档位；我们自己的结论直接当论述写；本地数字只作规模参考，不写成模型能力
- [ ] 3.4 无 SOTA 排名、无「谁最好」的表述；无未实现/待办标记
- [ ] 3.5 全文无固定数目（不写「四个子问题」「N 行」这类会腐坏的计数）
- [ ] 3.6 **无导语式论述**：没有「为什么重要」节，没有领域重要性/文档价值论证/总结回扣；全篇非问题内容只有标题下导语；**也不写“为何路线不能互相替代”这类分类式元讨论**
- [ ] 3.7 **缺口不丢**：原先记在各域「未解问题」里的 20 条缺口，逐条确认已在对应问题节里写到（删掉汇总节后不能出现“问题没了”）
- [ ] 3.8 **无工程问题**：正文不出现交付规格/贴回、编码传输、管道拼接、模块契约与可替换性、部署运维、可观测性这类内容；出现就删或改写成算法/数据/评测层面的问题
- [ ] 3.9 缩写首次出现给全称

## 4. 章序与相邻文件

- [ ] 4.1 新篇 `order: 20`（用 `scripts/docs_order.py insert --after 数字人介绍与技术路线 --create` 完成；**不改动其他文件**）
- [ ] 4.2 `总结.md` 计数行：「复盘文档 8 篇」→「复盘文档 8 篇 + 领域问题 1 篇」
- [ ] 4.3 站内链接命名空间统一为 `[[数字人概述/xxx|label]]`

## 5. 校验

- [ ] 5.1 全部站内 `[[...]]` 链接逐条验证目标文件存在（脚本 + 人工各一遍）
- [ ] 5.2 `python3 scripts/docs_order.py list management/docs/数字人概述`：9 篇按 10–90 排列、无重复 order
- [ ] 5.3 重启后端：`GET /api/management/docs` 中 `数字人概述/` 分组顺序为 数字人介绍与技术路线 → **数字人领域问题** → 数字人身份 → …
- [ ] 5.4 浏览器打开该文档：章节列表（TOC）到三级标题、3 个 Mermaid 正常渲染、表格不溢出、内链可跳转
- [ ] 5.5 独立复核（第二遍）：正文数字与来源抽查 10 条，全部可追溯

## 6. 提交

- [ ] 6.1 `openspec validate docs-dh-field-problems --strict` 通过
- [ ] 6.2 提交（`docs: 新增《数字人领域问题》—— 身份/动作/实时性/路线/跨域五块的问题清单`）
- [ ] 6.3 归档 change
