## 1. 设计与素材确认

- [x] 1.1 ✅ 已定：本 change **不归档**（用户确认，见 design `## Resolved Decisions`）；标题取 A、`summary` 不改、落点章末、清单不合并、轻微劣化归质量通道容忍带（默认取值，可否决）。
- [x] 1.2 ✅ 新增 9 篇引用已用 OpenAlex 直连核实到书目级；正文只内联点名，未引用其数字，因此无需全文核实。
- [x] 1.3 ✅ 已核对「一、身份」既有四个小节的数字（0.37→0.74、0.85、2.7 倍、1.25–4 倍、10,000+ 秒、7/16/9 视角等）并列出替代清单：新节正文**零阿拉伯数字**，只以"本章前面的量尺分歧"指向。

## 2. 正文新增小节

- [x] 2.1 新增小节开头写"现有判定手段都是借来的"与两类支线（卡通/动漫身份识别、面向生成的身份编码器）的边界。
- [x] 2.2 写不变性清单表（口型 / 表情 / 姿态 / 说话风格 / 机位 / 光照背景 / 风格化 / 正常演化，8 条）。
- [x] 2.3 写敏感性清单表（换人与几何漂移 / 画质劣化 / 尺度漂移 / 遮挡塌陷 / 跨机位重置 / 多人归属 / 时序量，7 条）；按 Resolved Decision 5 不合并遮挡与劣化。
- [x] 2.4 写"两条敏感轴必须分开"与三通道输出，配 Mermaid 图（合法→忽略、非法→身份轴/质量轴）。
- [x] 2.5 写验收协议四条硬性要求，并点名可复用素材（StyleID / ID-Sim / MagFace / FCB / 卡通与动漫人脸识别）。
- [x] 2.6 写代价与未决（数据瓶颈、角色同一性 vs 真人核验、底座选型）。
- [x] 2.7 按 Resolved Decision 3 保持 frontmatter `summary` 不变；未新增正式参考文献列表。

## 3. 验证与交付

- [x] 3.1 校验 frontmatter 未改、标题层级与章编号未变、"一、身份"下小节顺序为长视频漂移 → 多参考 → 分镜多视角 → 说话风格 → 风格化 → 本新节；两张新表列数与分隔行一致。
- [x] 3.2 核验站内引用：新节内以名称引用 StyleID / ID-Sim / FCB / MagFace 等库内已存在内容，未新增失效链接。
- [x] 3.3 检查 Mermaid：4 个块围栏配对，节点与边标签均无单 `$`；边标签按速查表用 `|文本|`（去掉直引号，避免与全文弯引号风格冲突）。
- [x] 3.4 确认无数字复述：新节阿拉伯数字计数为 0；引号风格与全文一致（直引号 0 处）。
- [x] 3.5 `openspec validate docs-dh-field-identity-discriminator --strict` 通过；经 FastAPI 详情接口确认 `数字人领域问题` 可读（content 20,754 字符，含新节标题）。
- [x] 3.6 提交 `docs(field-problems): ...`，仅包含预期文件。

## 4. 长期演进（本 change 不归档）

- [x] 4.1 在 design 记录"不归档"的结论与六条长期演进约定（同 change 追加、条目形式、引用核实、只增不改、无完成终点、导航问题另提 change）。
- [ ] 4.2 后续每轮新增问题条目时：先在本 change 的 design 追加小节规格与新增引用白名单，再追加 tasks 任务组，用户确认后才写正文。（standing 规则，适用于后续轮次）
- [x] 4.3 本轮补充完成后**未**跑 `openspec-archive-change`，符合约定；是否归档由用户单独决定。
