## 1. 证据复核

- [x] 1.1 在 `knowledge/Avatar Forcing 微调实践.md` 逐项复核 c1 / 300 秒的模长三段表、夹角与最近邻峰值、A1 CSIM 0.645 → 0.935，确认与 design D3/D5 数字一致。
- [x] 1.2 复核 offset / rolling KV cache / 推理期不重读身份锚帧的描述与《Avatar Forcing 模型精读》、论文方法节一致；确认静音段写作「工程观察 + 机制解释」，不写定量因果结论。

## 2. 目标架构落地

- [x] 2.1 在 `## 局限与启发` 内、`### 论文自己承认的局限` 之后新增 `### 长时身份漂移：历史递归、方向游走与静音放大`。
- [x] 2.2 新小节写入“机制”段：生成结果经 offset / KV cache 成为下一段历史 → 开放环误差积累 → 固定 $z_S$ 不等于解码身份恒定；配 1 张 Mermaid 反馈回路图。
- [x] 2.3 新小节写入“诊断”段：模长与夹角公式 + 符号解释 + 模长/方向对照表 + A1 对照 + c1 单身份边界声明。
- [x] 2.4 新小节写入“静音”段，标注证据等级；写入“治理指向”段，仅保留 A1 与该单例结果。
- [x] 2.5 将 `### 我们的实测（这条线是论文留的「未来工作」，我们把它做成了实验）` 改名为 `### 我们的实测：治理与适配`，删去其中已上移的“漂移诊断”条目，保留治理裁决 4 项、中文音频适配与微调注入点，并在开头指向新小节。
- [x] 2.6 按 design 的“跨节一致性触点”补两处锚点链接：`## 推理与系统链路` 末尾、`### 可操作启发` 第 3 条。

## 3. 全文一致性

- [x] 3.1 `## 术语与符号表` 补 `r_d` 与 $\theta_t$ 两行，且与新小节记号写法一致。
- [x] 3.2 检查全文：漂移诊断只在新小节出现一次；图片编号仍为 1–6、未新增资产；`## 相关文档` 未新增条目。

## 4. 校验

- [x] 4.1 运行论文笔记校验（frontmatter / 标题层级 / 公式 / 图片 URL / 图号 / sidecar / 内链）。
- [x] 4.2 在文档页实点两个新增锚点链接，确认 `#长时身份漂移历史递归方向游走与静音放大` slug 可用。
- [x] 4.3 运行 `openspec validate docs-avatar-forcing-drift-analysis --strict` 并记录结果。
